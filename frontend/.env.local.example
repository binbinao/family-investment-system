# 本地开发（npm run dev + 本机 uvicorn :8000）
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# 若前端也在 Docker 里、经 Nginx 访问，请与项目根 .env 中 NGINX_HOST_PORT 一致，例如 8888：
# NEXT_PUBLIC_API_URL=http://localhost:8888/api/v1
